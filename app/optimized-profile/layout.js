import { Metadata } from 'next';

export const metadata = {
  title: 'Optimized Profile Card | Khayyis Portfolio',
  description: 'A demonstration of the optimized profile card component with performance optimizations',
  keywords: ['profile card', 'web performance', 'optimization', 'Next.js', 'React'],
};

export default function OptimizedProfileLayout({ children }) {
  return children;
}